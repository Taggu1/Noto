import 'package:flutter/material.dart';

class SyncPageContainer extends StatelessWidget {
  final Widget child;
  const SyncPageContainer({super.key, required this.child});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
